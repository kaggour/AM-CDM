uri "http://kdl.ge.com/AM-CDM-system" alias AM-CDM-system version "$Revision:$ Last modified on   $Date:$".

import "http://kdl.ge.com/AM-CDM-process".

System (note "A manufacturing system that may be an aggregate of subsystems.") is a class,
	described by systemID (note "Unique identifier of the system.") with a single value of type string,
	described by systemName (note "The name of the system.") with a single value of type string,
	described by systemPurchaseDate (note "The purchase date of the system.") with a single value of type dateTime,
	described by systemOwner (note "The owning organization of the system.") with a single value of type Organization,
	described by systemLocation (note "The location where the system is installed.") with a single value of type Address,
	described by systemSpecification (note "Information about the system that is specified by the system manufacturer.") with a single value of type SystemSpecification,
	described by systemProcessHistory (note "An ordered series of processes that have occurred to the system.") with a single value of type ProcessHistory,
	described by systemComponents (note "Other systems that have been assembled and integrated to consitiute the system.") with values of type System,
	described by systemCapability (note "Capabilities and limitations of the system.") with a single value of type systemCapability.

SystemSpecification (note "Information about the system that is specified by the system manufacturer.") is a class,
	described by systemSerialNumber (note "The serial number of the system defined by the manufacturer.") with a single value of type string,
	described by systemModel (note "Model name of a machine defined by the machine manufacturer.") with a single value of type string,
	described by systemManufacturer (note "The manufacturer of the equipment.") with a single value of type Organization,
	described by systemSpecElectricVoltage (note "Voltage for an AM Machine's electrical connection.") with a single value of type Quantity,
	described by systemSpecPowerIntensity (note "Current intensity required to power an AM Machine.") with a single value of type Quantity,
	described by systemSpecPowerFrequency (note "Power frequency for the electrical connection of an AM Machine.") with a single value of type Quantity,
	described by systemSoftware (note "Computer programs required for operation of this system.") with values of type Software.

AMSystem (note "A system that is capable of additively manufacturing parts.") is a type of System.

systemSpecification of AMSystem only has values of type AMSystemSpecification.
systemCapability of AMSystem only has values of type AMSystemCapability.
 
AMSystemSpecification (note "Specifications that are unique to an AM system.") is a type of SystemSpecification,
	described by amSystemHazmatRequirements (note "A document of a link to AM system hazardous material requirements.") with a single value of type string.

Sensor (note "A system that observes an object or a process and collects data about the object or process.") is a type of System,
	described by sensorSpecification (note "A specification that is unique to a sensor.") with a single value of type SensorSpecification.

SensorSpecification (note "A specification that is unique to a sensor.") is a type of SystemSpecification,
	described by samplingRateMin (note "The minimum rate at which the sensor is able to sample data at.") with a single value of type Quantity,
	described by samplingRateMax (note "The maximum rate at which the sensor is able to sample data at.") with a single value of type Quantity.

BuildChamber (note "An enclosed location within an AM system where the parts are fabricated.") is a type of System,
	described by buildChamberEnvironmentalControl (note "Environment control of the build chamber, indluding its temperature, pressure, humidity and oxygen level, for optimal quality of AM material transformation.") with a single value of type BuildEnvironmentalControl,
	described by buildChamberVolume (note "Total usable volume available in the AM system for building parts.") with a single value of type BuildVolume.

Laser (note "A system that is capable of producing a laser beam.") is a type of System,
	described by laserSpecification (note "A specification that is unique to a laser.") with a single value of type laserSpecification,
	described by laserLaserBeamProfile (note "The measured cross-sectional intensity profile of a laser beam.") with a single value of type Document.

laserSpecification (note "A specification that is unique to a laser.") is a type of SystemSpecification,
	// removed because an AM system can have multiple laser systems associated with it. A single laser can not have a number of lasers. The number of lasers would have to be deduced by the number of laser systems an AM system has. we could group the energy systems together to capture metadata in the future. described by laserNumberOfLasers (note "The number of laser beams the laser is intentionally designed to produce by the laser manufacturer.") with a single value of type int,
	described by laserModeOfLaserOperation (note "The mode that the laser operates at. For example, Continuous Wave, Pulsed Wave, etc.") with a single value of type string,
	described by laserMaxLaserPower (note "The maximum power output that the laser is able to emmit, as specified by the laser manufacturer.") with a single value of type Quantity,
	described by laserMinLaserPower (note "The minimum power output that the laser is able to emmit, as specified by the laser manufacturer.") with a single value of type Quantity,
	described by laserRatedWaveLength (note "The wavelength that the laser operates at as specificed by the laser manufacturer.") with a single value of type Quantity,
	described by laserBeamShape (note "The shape of the laser beam that was designed by the laser manufacturer. This does not account for any additional optics that may modify the laser beam shape.") with a single value of type string,
	described by laserMaxBeamSpotSize (note "The maximum cross-sectional size of the laser beam as specificed by the laser manufacturer. This does not account for any additional optics that may modify the spot size.") with a single value of type Quantity,
	described by laserMinBeamSpotSize (note "The minimum cross-sectional size of the laser beam as specificed by the laser manufacturer. This does not account for any additional optics that may modify the spot size.") with a single value of type Quantity,
	described by laserFocusCorrectionType (note "The optical laser focusing correction type(s), such as F-theta lens and/or Dynamic Focus Module (DFM).") with a single value of type string.

Recoater (note "A system which spreads or recoats powder accross a surface.") is a type of System.

systemSpecification of Recoater only has values of type RecoaterSpecification.

RecoaterSpecification (note "A specification that is unique to a recoater.") is a type of SystemSpecification,
	described by samplingRateMin (note ".") with a single value of type Quantity,
	described by samplingRateMax (note ".") with a single value of type Quantity,
	described by recoaterMaximumSpeed (note "The maximum speed at which the recoater can transverse at.") with a single value of type Quantity,
	described by recoaterMaxUsesAllowed (note "The maximum number of builds that a recoater is allowed to participate in.") with a single value of type integer,
	described by recoatingSurfaceMaterialType (note "The material that the recoating surface is made of.") with a single value of type MaterialType,
	described by recoatingSurfaceMaterialSpecification (note "Specifications that are unique to the recoating surface material.") with a single value of type Document.

GalvoScanner (note "A system that is used to manipulate the position of something, such as a laser beam.") is a type of System.

systemSpecification of GalvoScanner only has values of type GalvoScannerSpecification.

GalvoScannerSpecification (note "A specification that is unique to a galvo scanner.")is a type of SystemSpecification,
	described by galvoScannerMaxScanAngleX (note "The maximum angle in the X direction, relative to the galvo scanner origin, that the galvo scanner is capable of rotating to.") with a single value of type Quantity,
	described by galvoScannerMaxScanAngleY (note "The maximum angle in the Y direction, relative to the galvo scanner origin, that the galvo scanner is capable of rotating to..") with a single value of type Quantity,
	described by galvoScannerRepeatability (note "The maximum angle in the Z direction, relative to the galvo scanner origin, that the galvo scanner is capable of rotating to..") with a single value of type Quantity,
	described by galvoScannerMaxScanSpeed (note "The maximum taversal speed that the galvo scanner is capable of achieving.") with a single value of type Quantity,
	described by galvoScannerOrigin (note "The location of the origin of the galvo scanner within the scan area, typically where the galvanometer motors recieve a neutral control signal.") with a single value of type CartesianCoordinate,
	described by galvoScannerType (note "The type of galvo scanner.") with a single value of type string.

Camera (note ".") is a type of Sensor.

systemSpecification of Camera only has values of type CameraSpecification.

CameraSpecification (note "A specification that is unique to a camera.") is a type of SensorSpecification,
	described by cameraImageWidth (note "Camera image width in pixel.") with a single value of type Quantity,
	described by cameraBitDepth (note "Bit depth of pixel values.") with a single value of type Quantity,
	described by cameraImageHeight (note "Camera image height in pixel.") with a single value of type Quantity,
	described by cameraCenter (note "Width, Height center position of a camera in pixels.") with a single value of type CartesianCoordinate,
	described by cameraOpticalFilterBandwidth (note "Bandwidth of a camera's filter bandwidth.") with a single value of type Quantity,
	described by cameraDetectorPitchSize (note "Camera detection pitch size.") with a single value of type Quantity,
	described by cameraDigitalMagnification (note "Magnification factor setting of a camera.") with a single value of type Quantity,
	described by cameraLensOpticalMagnification with a single value of type float,
	described by cameraMinShutterSpeed (note "Shutter speed of a camera.") with a single value of type Quantity.

Lens (note "An assembly of shaped glass or plastic elements designed to collect, bend, and focus incoming light rays onto a single plane.") is a type of Sensor.

systemSpecification of Lens only has values of type LensSpecification.

LensSpecification (note "A specification that is unique to a lens.") is a type of SensorSpecification,
	described by lensAperture (note "The maximum aperture of the lens.") with a single value of type Quantity,
	described by lensFilter (note "The type of filter used in conjunction with a lens to modify the light that passes through it.")with a single value of type string.

Photodiode (note "A semiconductor light sensor that converts light energy (photons) directly into an electrical current or voltage.") is a type of Sensor.

systemSpecification of Photodiode only has values of type PhotodiodeSpecification.

PhotodiodeSpecification (note "A specification that is unique to a photodiode.") is a type of SensorSpecification,
	described by photodiodeOpticalFilterBandwidth (note ".") with a single value of type Quantity,
	described by photodiodeBitDepth (note ".") with a single value of type Quantity.

Pyrometer (note "a non-contact temperature measurement device that operates by detecting and measuring radiation emitted from an object.") is a type of Sensor.

systemSpecification of Pyrometer only has values of type PyrometerSpecification.

PyrometerSpecification (note "A specification that is unique to a pyrometer.") is a type of SensorSpecification,
	described by pyrometerOpticalFilterBandwidth1 (note "The first bandwidth of the pyrometers optical filter") with a single value of type Quantity,
	described by pyrometerOpticalFilterBandwidth2 (note "The second bandwidth (if applicable) of the pyrometers optical filter.") with a single value of type Quantity,
	described by pyrometerBitDepth (note "The resolution of the pyrometer representing the number of digital increments that are available to translate the anolog signal into a digital temperature reading.") with a single value of type Quantity.

LaserPowderBedFusionSystem (note "An Additive Manufacturing system capable of building parts through the laser powder bed fusion process.") is a type of AMSystem.

ElectronBeamPowderBedFusionSystem (note "An Additive Manufacturing system capable of building parts through the electron beam powder bed fusion process.") is a type of AMSystem.

DirectedEnergyDepositionSystem (note "An Additive Manufacturing system capable of building parts through the directed energy deposition process.") is a type of AMSystem.

BinderJetSystem (note "An Additive Manufacturing system capable of building parts through the binder jet process.") is a type of AMSystem.

BuildEnvironmentalControl (note "Environment control of the build chamber on its temperature, pressure, humidity and oxygen level, for optimal quality of AM material transformation.") is a class,
	described by buildEnvironmentControlShieldingGasType (note "The type of shielding gas used in the build environmental control system.") with a single value of type string,
	described by buildEnvironmentControlMaximumPreheatingTemperature (note "The maximum temperature that the build environmental control is capable of preheating to.") with a single value of type Quantity,
	described by buildEnvironmentControlGasFlowNozzleType (note "The type of gas flow nozzle utilized by the build environmental control.") with a single value of type string,
	described by buildEnvironmentControlShieldingDeviceType (note "The type of shielding device used by the build environmental control.") with a single value of type string.

BuildVolume (note "Total usable volume available as part of the AM system for building parts.") is a class,
	described by buildVolumeShape (note "Shape of a building volume, i.e., 'box' or 'cylinder'.") with a single value of type string,
	described by buildVolumeDimension (note "Width, Depth, Height of a building volume.") with a single value of type CartesianCoordinate,
	described by buildVolumeDiameter (note "Diameter of a building volume if of cylinder type.") with a single value of type Quantity,
	described by buildVolumeMachineOrigin (note "Origin X,Y,Z as defined by machine manufacturer relative to build volume center.") with a single value of type CartesianCoordinate,
	described by buildVolumeTotalBuildVolume (note "The total usable volume available in the AM System.") with a single value of type Quantity,
	described by buildVolumeMachineLastCleanoutDate (note "The date when the build volume was last cleaned.") with a single value of type dateTime.

SystemCapability (note "Capabilities or limitations of a system, which may be provided by the system manufacturer or vendor.") is a class

AMSystemCapability (note "Capabilities or limitations of an AM system, which may be provided by the system manufacturer or vendor.") is a class,
	described by systemCapabilityProcessableRawMaterial (note "A processable raw material list of an AM system.") with values of type MaterialType,
	described by systemCapabilityMaximumBuildRate (note "The maximum build rate (reported in volume or mass per unit of time) that the AM system is capable of operating at.") with a single value of type Quantity,
	described by systemCapabilityMinimumLayerThickness (note "The smallest layer thickness that the AM system is capable of producing.") with a single value of type Quantity,
	described by systemCapabilityMaximumLayerThickness (note "The maximum layer thickness that the AM system is capable of producing.") with a single value of type Quantity,
	described by systemCapabilityMinimumOxygenLevel (note "The minimum level of oxygen that the AM system is capable of operating at.") with a single value of type Quantity,
	described by systemCapabilityMinimumMoistureLevel (note "The minimum level of moisture that the AM system is capable of operating at.") with a single value of type Quantity.

SystemQualificationEvent (note "An event that a system undergoes to evaluate whether or not it can be qualified.") is a type of ProcessStep,
	described by processEventData (note "Process monitoring data, in-situ monitoring data, real-time monitoring etc, real-time control information.") with a single value of type SystemQualificationEventData.

SystemQualificationEventData (note "Information that is generated as a result of a system qualification event.") is a type of ProcessStepData,
	described by systemQualDataPassFail (note "Pass / fail result.") with a single value of type string,
	described by systemQualDataResults (note "A certification capturing the basic calibration and test build(s) performed after installation of machine; aka, suite acceptance report, tied to a machine serial number.") with a single value of type string.

InstallationQualification (note ".") is a type of SystemQualificationEvent.

OperationalQualification (note ".") is a type of SystemQualificationEvent.

PerformanceQualification (note ".") is a type of SystemQualificationEvent.

MaintenanceEvent is a (note ".") type of PlannedProcessStep,
	described by processEventData (note "Process monitoring data, in-situ monitoring data, real-time monitoring etc, real-time control information.") with a single value of type MaintenanceEventData.

MaintenanceEventData (note ".") is a type of ProcessStepData,
	described by maintenanceDataReport (note "Technical report from the maintenance.") with a single value of type Document.

SystemCalibration (note "Calibration information.") is a type of MaintenanceEvent.

processEventData of SystemCalibration only has values of type SystemCalibrationData.

SystemCalibrationData (note ".") is a type of ProcessStepData,
	described by calibrationPassFail (note "Pass / fail result.") with a single value of type string,
	described by calibrationDataResults (note "The results of the equipment calibration (files, etc.).") with a single value of type Document.

Software (note ".") is a class,
	described by softwareName (note "A unique identifier for the software.") with a single value of type string,
	described by softwareID (note "Version/configuration/OS.") with a single value of type string,
	described by softwareVendor (note "Software Vendor identification. Links to Organization foreign key.") with a single value of type Organization,
	described by softwareVersion (note "The version number of the software.") with a single value of type string,
	described by softwareConfigurationSettingsFile (note "The file representing the specific software settings used.") with a single value of type Document,
	described by softwareFileType (note "File type (for automated processing).") with a single value of type string.

HTSystem (note ".") is a type of System.
HIPSystem (note ".") is a type of System.
PartExtractionSystem (note ".") is a type of System.
